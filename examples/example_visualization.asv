clear all; close all; clc;

data = load('PGRNKO_Example.mat');

%%
trace =- 
% Plot traces
CIA.Visualization.plotTraces(data.trace, data.spikeTrain);

% Plot spike train
CIA.Visualization.plotSpikeTrain(data.trace, data.spikeTrain, 'heatmap', true);